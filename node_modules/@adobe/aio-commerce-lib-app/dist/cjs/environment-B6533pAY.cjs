/**
 * @license
 *
 * Copyright 2026 Adobe. All rights reserved.
 * This file is licensed to you under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License. You may obtain a copy
 * of the License at http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software distributed under
 * the License is distributed on an "AS IS" BASIS, WITHOUT WARRANTIES OR REPRESENTATIONS
 * OF ANY KIND, either express or implied. See the License for the specific language
 * governing permissions and limitations under the License.
 */

const require_rolldown_runtime = require('./rolldown-runtime-CDQd2xEI.cjs');
let _adobe_aio_commerce_lib_core_error = require("@adobe/aio-commerce-lib-core/error");
let valibot = require("valibot");
valibot = require_rolldown_runtime.__toESM(valibot, 1);
let _adobe_aio_commerce_lib_core_commerce = require("@adobe/aio-commerce-lib-core/commerce");

//#region ../../packages-private/common-utils/source/valibot/utils.ts
/**
* Parses the input using the provided schema and throws a {@link CommerceSdkValidationError} error if the input is invalid.
* @param schema - The schema to use for parsing.
* @param input - The input to parse.
* @param message - Optional custom error message for the validation error.
*/
function parseOrThrow(schema, input, message) {
	const result = valibot.safeParse(schema, input);
	if (!result.success) throw new _adobe_aio_commerce_lib_core_error.CommerceSdkValidationError(message ?? "Invalid input", { issues: result.issues });
	return result.output;
}

//#endregion
//#region source/config/lib/environment.ts
/**
* Returns true when an item applies to the given Commerce environment: either it
* declares no `env` (applies to all environments) or its `env` list includes the
* environment. This is the single keep-rule shared by install-time filtering and
* the environment-filtered `app-config` display path.
*
* @param item - The webhook or event entry, which may carry an optional `env` array.
* @param env - The target Commerce environment (e.g. `"paas"` or `"saas"`).
*/
function appliesToEnv(item, env) {
	return item.env === void 0 || item.env.includes(env);
}
/**
* Reads and validates the target Commerce environment from the install workflow params,
* where the router maps the request's `commerceEnv` onto `AIO_COMMERCE_API_FLAVOR`.
* Throws if the value is absent or not a recognised Commerce environment.
*
* @param params - The runtime action params available to install steps.
*/
function getInstallCommerceEnv(params) {
	return parseOrThrow(_adobe_aio_commerce_lib_core_commerce.CommerceEnvSchema, params.AIO_COMMERCE_API_FLAVOR, "Missing or unknown commerce environment");
}
/**
* Filters a provider list to the given environment: each provider keeps only its
* applicable events, and providers left with no events are dropped entirely.
*/
function filterProvidersByEnv(providers, env) {
	return providers.map((provider) => ({
		...provider,
		events: provider.events.filter((event) => appliesToEnv(event, env))
	})).filter((provider) => provider.events.length > 0);
}
/**
* Returns a copy of a validated app config scoped to the given Commerce environment:
* business-config fields and webhooks not applicable to the environment are removed,
* each event provider keeps only its applicable events, and providers left with no
* events are dropped. Uses the same `appliesToEnv` keep-rule as the `config` action,
* so the `app-config` action renders the same scoped set that the rest of the SDK
* (install and `config`) applies.
*
* @param config - The validated app config.
* @param env - The target Commerce environment (e.g. `"paas"` or `"saas"`).
*/
function filterAppConfigByEnv(config, env) {
	const filtered = { ...config };
	if (filtered.businessConfig && Array.isArray(filtered.businessConfig.schema)) filtered.businessConfig = {
		...filtered.businessConfig,
		schema: filtered.businessConfig.schema.filter((field) => appliesToEnv(field, env))
	};
	if (Array.isArray(filtered.webhooks)) filtered.webhooks = filtered.webhooks.filter((webhook) => appliesToEnv(webhook, env));
	if (filtered.eventing) filtered.eventing = {
		...filtered.eventing,
		...filtered.eventing.commerce && { commerce: filterProvidersByEnv(filtered.eventing.commerce, env) },
		...filtered.eventing.external && { external: filterProvidersByEnv(filtered.eventing.external, env) }
	};
	return filtered;
}
/**
* Filters a business configuration schema to the fields applicable to the
* given Commerce environment. Fields without an `env` property apply to all
* environments and are always included.
*
* @param schema - The business configuration schema to filter.
* @param env - The Commerce environment to filter by.
*/
function filterSchemaByEnv(schema, env) {
	return schema.filter((field) => appliesToEnv(field, env));
}

//#endregion
Object.defineProperty(exports, 'appliesToEnv', {
  enumerable: true,
  get: function () {
    return appliesToEnv;
  }
});
Object.defineProperty(exports, 'filterAppConfigByEnv', {
  enumerable: true,
  get: function () {
    return filterAppConfigByEnv;
  }
});
Object.defineProperty(exports, 'filterSchemaByEnv', {
  enumerable: true,
  get: function () {
    return filterSchemaByEnv;
  }
});
Object.defineProperty(exports, 'getInstallCommerceEnv', {
  enumerable: true,
  get: function () {
    return getInstallCommerceEnv;
  }
});
Object.defineProperty(exports, 'parseOrThrow', {
  enumerable: true,
  get: function () {
    return parseOrThrow;
  }
});