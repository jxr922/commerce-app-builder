"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
/*************************************************************************
 * Copyright 2024 Adobe
 * All Rights Reserved.
 *
 * NOTICE: Adobe permits you to use, modify, and distribute this file in
 * accordance with the terms of the Adobe license agreement accompanying
 * it. If you have received this file from a source other than Adobe,
 * then your use, modification, or distribution of it requires the prior
 * written permission of Adobe.
 **************************************************************************/
const interframe_1 = require("../interframe");
const Global_1 = require("../src/Global");
const interframe_2 = __importDefault(require("../interframe"));
describe('interFrame', () => {
    const handler = () => { };
    const interframeMock = {
        emit: (type, evt) => ({ evt, type }),
        off: (type, handler) => ({ handler, type }),
        on: (type, handler) => ({ handler, type }),
        send: (action, callbacks, data) => ({ action, callbacks, data })
    };
    beforeEach(() => {
        jest.clearAllMocks();
        const emr = (0, Global_1.getEMR)();
        // Add interFrame to emr before spying on it
        emr.interFrame = () => interframeMock;
        jest.spyOn(emr, 'interFrame');
    });
    it('can emit()', () => {
        const spy = jest.spyOn(interframeMock, 'emit');
        const payload = {
            callbacks: {
                sendCompleted: jest.fn(),
                sendError: jest.fn(),
                sendProgress: jest.fn()
            },
            data: { test: 'data' }
        };
        interframe_2.default.emit('CREATE', payload);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('CREATE', payload);
    });
    it('can off()', () => {
        const spy = jest.spyOn(interframeMock, 'off');
        interframe_2.default.off('CREATE', handler);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('CREATE', handler);
    });
    it('can on()', () => {
        const spy = jest.spyOn(interframeMock, 'on');
        interframe_2.default.on('CREATE', handler);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('CREATE', handler);
    });
    it('can send() with callbacks', () => {
        const spy = jest.spyOn(interframeMock, 'send');
        const callbacks = {
            onComplete: jest.fn(),
            onError: jest.fn(),
            onProgress: jest.fn()
        };
        interframe_2.default.send('CREATE', callbacks, { name: 'test' });
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('CREATE', callbacks, { name: 'test' }, undefined);
    });
    it('can send() without callbacks', () => {
        const spy = jest.spyOn(interframeMock, 'send');
        interframe_2.default.send('UPDATE', undefined, { id: 1 });
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('UPDATE', undefined, { id: 1 }, undefined);
    });
    it('can send() without data', () => {
        const spy = jest.spyOn(interframeMock, 'send');
        interframe_2.default.send('DELETE');
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('DELETE', undefined, undefined, undefined);
    });
    it('can send() to a specific target', () => {
        const spy = jest.spyOn(interframeMock, 'send');
        interframe_2.default.send('CREATE', undefined, { name: 'test' }, interframe_1.FrameTarget.RAIL);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('CREATE', undefined, { name: 'test' }, interframe_1.FrameTarget.RAIL);
    });
});
//# sourceMappingURL=interframe.test.js.map