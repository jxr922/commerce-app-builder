"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
/*************************************************************************
 * Copyright 2021 Adobe
 * All Rights Reserved.
 *
 * NOTICE: Adobe permits you to use, modify, and distribute this file in
 * accordance with the terms of the Adobe license agreement accompanying
 * it. If you have received this file from a source other than Adobe,
 * then your use, modification, or distribution of it requires the prior
 * written permission of Adobe.
 **************************************************************************/
const ai_1 = __importStar(require("../ai"));
const Global_1 = require("../src/Global");
describe('ai', () => {
    const evt = {};
    const handler = () => { };
    const aiMock = {
        appContext: () => Promise.resolve({ mainAppAgentApplicationId: ai_1.AIApplicationId.AEM, mainAppId: 'test-app', mainAppRoot: 'test-root' }),
        closeChat: () => { },
        context: {},
        emit: (type, evt) => ({ evt, type }),
        getDocumentationSources: () => __awaiter(void 0, void 0, void 0, function* () { return ({ [ai_1.AIApplicationId.AEP]: 'Experience Platform' }); }),
        off: (type, handler) => ({ handler, type }),
        on: (type, handler) => ({ handler, type }),
        onContextRequest: (callback) => callback(),
        openChat: (queryOrProps, shouldSubmit) => {
            const submit = shouldSubmit !== null && shouldSubmit !== void 0 ? shouldSubmit : true;
            if (typeof queryOrProps === 'string') {
                return { query: queryOrProps, shouldSubmit: submit };
            }
            return queryOrProps ? Object.assign(Object.assign({}, queryOrProps), { shouldSubmit: queryOrProps.shouldSubmit }) : undefined;
        },
        sendMessage: (payload) => payload,
        setProvisioningButton: (callback) => callback,
        setUserPermission: (hasPermission) => hasPermission,
        showAssistant: (permissions) => permissions,
        showToast: (message, options) => ({ message, options }),
        toggleButtonActive: (active) => active,
        toggleButtonVisible: (visible) => visible,
        toggleExpansion: (isExpanded) => isExpanded,
        togglePanel: (isOpen) => isOpen
    };
    beforeEach(() => {
        jest.clearAllMocks();
        const emr = (0, Global_1.getEMR)();
        jest.spyOn(emr, 'ai').mockImplementation(() => aiMock);
    });
    it('can emit()', () => {
        const spy = jest.spyOn(aiMock, 'emit');
        ai_1.default.emit(ai_1.AIMessageType.RESPONSE, evt);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('RESPONSE', evt);
    });
    it('can getDocumentationSources()', () => {
        const spy = jest.spyOn(aiMock, 'getDocumentationSources');
        ai_1.default.getDocumentationSources();
        expect(spy).toHaveBeenCalledTimes(1);
    });
    it('can off()', () => {
        const spy = jest.spyOn(aiMock, 'off');
        ai_1.default.off('RESPONSE', handler);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('RESPONSE', handler);
    });
    it('can on()', () => {
        const spy = jest.spyOn(aiMock, 'on');
        ai_1.default.on('RESPONSE', handler);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('RESPONSE', handler);
    });
    it('can onContextRequest()', () => {
        const spy = jest.spyOn(aiMock, 'onContextRequest');
        ai_1.default.onContextRequest(() => Promise.resolve({}));
        expect(spy).toHaveBeenCalledTimes(1);
    });
    it('can openChat()', () => {
        const spy = jest.spyOn(aiMock, 'openChat');
        ai_1.default.openChat('How do I create a new project?', true);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('How do I create a new project?', true);
    });
    it('can openChat with conversationId', () => {
        const spy = jest.spyOn(aiMock, 'openChat');
        ai_1.default.openChat({ conversationId: 'conv-123', query: 'How do I create a new project?' });
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith({ conversationId: 'conv-123', query: 'How do I create a new project?' }, undefined);
    });
    it('can openChat with conversationId and populate query', () => {
        const spy = jest.spyOn(aiMock, 'openChat');
        ai_1.default.openChat({ conversationId: '0x123', query: 'Trend events for last month?', shouldSubmit: false });
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith({ conversationId: '0x123', query: 'Trend events for last month?', shouldSubmit: false }, undefined);
    });
    it('can openChat with appContext', () => {
        const spy = jest.spyOn(aiMock, 'openChat');
        const appContext = { data: { insight: { content: 'test', id: '123' } } };
        ai_1.default.openChat({ appContext, query: 'hi' });
        expect(spy).toHaveBeenCalledWith({ appContext, query: 'hi' }, undefined);
    });
    it('can openChat with legacy signature (query string)', () => {
        const spy = jest.spyOn(aiMock, 'openChat');
        ai_1.default.openChat('How do I create a new project?', false);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('How do I create a new project?', false);
    });
    it('can openChat with legacy signature (query string, shouldSubmit)', () => {
        const spy = jest.spyOn(aiMock, 'openChat');
        ai_1.default.openChat('Pre-filled query', false);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('Pre-filled query', false);
    });
    it('can closeChat()', () => {
        const spy = jest.spyOn(aiMock, 'closeChat');
        ai_1.default.closeChat();
        expect(spy).toHaveBeenCalledTimes(1);
    });
    it('can sendMessage()', () => {
        const spy = jest.spyOn(aiMock, 'sendMessage');
        const message = {
            context: aiMock.context,
            type: ai_1.AIMessageType.CONTEXT
        };
        ai_1.default.sendMessage(message);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith(message);
    });
    it('can showAssistant()', () => {
        const spy = jest.spyOn(aiMock, 'showAssistant');
        ai_1.default.showAssistant(['permission1', 'permission2']);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith(['permission1', 'permission2']);
    });
    it('can togglePanel()', () => {
        const spy = jest.spyOn(aiMock, 'togglePanel');
        ai_1.default.togglePanel(true);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith(true);
    });
    it('can toggleExpansion()', () => {
        const spy = jest.spyOn(aiMock, 'toggleExpansion');
        ai_1.default.toggleExpansion(true);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith(true);
    });
    it('can toggleExpansion() with false', () => {
        const spy = jest.spyOn(aiMock, 'toggleExpansion');
        ai_1.default.toggleExpansion(false);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith(false);
    });
    it('can showToast()', () => {
        const spy = jest.spyOn(aiMock, 'showToast');
        ai_1.default.showToast('Hello, world!', { variant: 'positive' });
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('Hello, world!', { variant: 'positive' });
    });
    it('can appContext()', () => {
        const spy = jest.spyOn(aiMock, 'appContext');
        ai_1.default.appContext();
        expect(spy).toHaveBeenCalledTimes(1);
    });
    it('can setProvisioningButton()', () => {
        const spy = jest.spyOn(aiMock, 'setProvisioningButton');
        const callback = jest.fn();
        ai_1.default.setProvisioningButton(callback);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith(callback);
    });
    it('can setUserPermission()', () => {
        const spy = jest.spyOn(aiMock, 'setUserPermission');
        ai_1.default.setUserPermission(true);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith(true);
    });
    it('can toggleButtonActive()', () => {
        const spy = jest.spyOn(aiMock, 'toggleButtonActive');
        ai_1.default.toggleButtonActive(false);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith(false);
    });
    it('can toggleButtonVisible()', () => {
        const spy = jest.spyOn(aiMock, 'toggleButtonVisible');
        ai_1.default.toggleButtonVisible(true);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith(true);
    });
    it('can get context', () => {
        const testContext = { testKey: 'testValue' };
        aiMock.context = testContext;
        const result = ai_1.default.context;
        expect(result).toEqual(testContext);
    });
    it('can set context', () => {
        const testContext = { testKey: 'testValue' };
        ai_1.default.context = testContext;
        expect(aiMock.context).toEqual(testContext);
    });
});
//# sourceMappingURL=ai.test.js.map