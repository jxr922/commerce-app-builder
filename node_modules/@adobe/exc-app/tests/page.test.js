"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
/*************************************************************************
 * Copyright 2020 Adobe
 * All Rights Reserved.
 *
 * NOTICE: Adobe permits you to use, modify, and distribute this file in
 * accordance with the terms of the Adobe license agreement accompanying
 * it. If you have received this file from a source other than Adobe,
 * then your use, modification, or distribution of it requires the prior
 * written permission of Adobe.
 **************************************************************************/
const Global_1 = require("../src/Global");
const page_1 = __importStar(require("../page"));
describe('page/index', () => {
    const emr = (0, Global_1.getEMR)();
    beforeEach(() => {
        emr.page = {
            appContainer: 'testContainer',
            blockNavigation: (enabled, options) => {
                console.log(enabled, options); // eslint-disable-line no-console
            },
            done: () => { },
            favicon: 'test.ico',
            generateShellUrl: (location, newApp) => {
                console.log(location, newApp); // eslint-disable-line no-console
            },
            iframeReload: () => { },
            modal: true,
            notFound: () => { },
            preventDefaultCombos: [{ ctrlKey: true, key: 'A' }],
            reloadSandbox: () => { },
            shellRedirect: (path, options) => {
                console.log(path, options); // eslint-disable-line no-console
            },
            spinner: true,
            start: () => { },
            title: 'test title',
            unloadPromptMessage: 'Are you sure?',
            updateUrlContext: (contextKey) => {
                console.log(contextKey); // eslint-disable-line no-console
            }
        };
    });
    it('correctly sets up the connection with module-runtime', () => {
        const doneSpy = jest.spyOn(emr.page, 'done');
        page_1.default.done({ name: 'test' });
        expect(doneSpy).toHaveBeenCalledTimes(1);
        expect(doneSpy).toHaveBeenCalledWith({ name: 'test' });
        expect(page_1.default.appContainer).toBe('testContainer');
        page_1.default.appContainer = 'testingContainer';
        expect(emr.page.appContainer).toBe('testingContainer');
        const notFoundSpy = jest.spyOn(emr.page, 'notFound');
        page_1.default.notFound();
        expect(notFoundSpy).toHaveBeenCalledTimes(1);
        expect(page_1.default.favicon).toBe('test.ico');
        page_1.default.favicon = 'updated.ico';
        expect(emr.page.favicon).toBe('updated.ico');
        const iframeReloadSpy = jest.spyOn(emr.page, 'iframeReload');
        page_1.default.iframeReload();
        expect(iframeReloadSpy).toHaveBeenCalledTimes(1);
        expect(iframeReloadSpy).toHaveBeenCalledWith();
        page_1.default.iframeReload(false);
        expect(iframeReloadSpy).toHaveBeenCalledTimes(2);
        expect(iframeReloadSpy).toHaveBeenCalledWith(false);
        const generateShellUrlSpy = jest.spyOn(emr.page, 'generateShellUrl');
        page_1.default.generateShellUrl({ path: '/path' }, false);
        expect(generateShellUrlSpy).toHaveBeenCalledTimes(1);
        expect(generateShellUrlSpy).toHaveBeenCalledWith({ path: '/path' }, false);
        page_1.default.generateShellUrl({ path: '/path', sandbox: 'sbox' }, false);
        expect(generateShellUrlSpy).toHaveBeenCalledWith({ path: '/path', sandbox: 'sbox' }, false);
        expect(page_1.default.modal).toBe(true);
        page_1.default.modal = false;
        expect(emr.page.modal).toBe(false);
        const reloadSandboxSpy = jest.spyOn(emr.page, 'reloadSandbox');
        page_1.default.reloadSandbox();
        expect(reloadSandboxSpy).toHaveBeenCalledTimes(1);
        expect(page_1.default.preventDefaultCombos.length).toBe(1);
        expect(page_1.default.preventDefaultCombos[0].key).toBe('A');
        page_1.default.preventDefaultCombos = [{ ctrlKey: true, key: 'A' }, { altKey: false, key: 'B' }];
        expect(emr.page.preventDefaultCombos.length).toBe(2);
        expect(page_1.default.preventDefaultCombos[1].key).toBe('B');
        const shellRedirectSpy = jest.spyOn(emr.page, 'shellRedirect');
        page_1.default.shellRedirect('test path');
        expect(shellRedirectSpy).toHaveBeenCalledTimes(1);
        expect(shellRedirectSpy).toHaveBeenCalledWith('test path');
        page_1.default.shellRedirect('new path', { discovery: true });
        expect(shellRedirectSpy).toHaveBeenCalledTimes(2);
        expect(shellRedirectSpy).toHaveBeenCalledWith('new path', { discovery: true });
        page_1.default.shellRedirect('new path', { replace: false });
        expect(shellRedirectSpy).toHaveBeenCalledTimes(3);
        expect(shellRedirectSpy).toHaveBeenCalledWith('new path', { replace: false });
        const redirectUrl = 'https://experience.adobe.com/#/@tenant/co:collaboratorid/rtcdp-collaboration/';
        page_1.default.shellRedirect(redirectUrl, { replace: false });
        expect(shellRedirectSpy).toHaveBeenCalledTimes(4);
        expect(shellRedirectSpy).toHaveBeenCalledWith(redirectUrl, { replace: false });
        page_1.default.shellRedirect('/co:newcollab/test', { replace: false });
        expect(shellRedirectSpy).toHaveBeenCalledTimes(5);
        expect(shellRedirectSpy).toHaveBeenCalledWith('/co:newcollab/test', { replace: false });
        expect(page_1.default.spinner).toBe(true);
        page_1.default.spinner = false;
        expect(emr.page.spinner).toBe(false);
        expect(page_1.default.title).toBe('test title');
        page_1.default.title = 'updated title';
        expect(emr.page.title).toBe('updated title');
        const blockNavSpy = jest.spyOn(emr.page, 'blockNavigation');
        page_1.default.blockNavigation(true);
        expect(blockNavSpy).toHaveBeenCalledTimes(1);
        expect(page_1.default.unloadPromptMessage).toBe('Are you sure?');
        page_1.default.unloadPromptMessage = 'But are you sure you are sure?';
        expect(emr.page.unloadPromptMessage).toBe('But are you sure you are sure?');
        const updateContextSpy = jest.spyOn(emr.page, 'updateUrlContext');
        page_1.default.updateUrlContext({ contextKey: page_1.UrlContextKey.COLLABORATOR });
        expect(updateContextSpy).toHaveBeenCalledTimes(1);
        expect(updateContextSpy).toHaveBeenCalledWith({ contextKey: page_1.UrlContextKey.COLLABORATOR });
        const startSpy = jest.spyOn(emr.page, 'start');
        page_1.default.start({ name: 'test' });
        expect(startSpy).toHaveBeenCalledTimes(1);
        expect(startSpy).toHaveBeenCalledWith({ name: 'test' });
    });
});
//# sourceMappingURL=page.test.js.map