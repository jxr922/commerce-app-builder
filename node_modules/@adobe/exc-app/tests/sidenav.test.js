"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
/*************************************************************************
 * Copyright 2021 Adobe
 * All Rights Reserved.
 *
 * NOTICE: Adobe permits you to use, modify, and distribute this file in
 * accordance with the terms of the Adobe license agreement accompanying
 * it. If you have received this file from a source other than Adobe,
 * then your use, modification, or distribution of it requires the prior
 * written permission of Adobe.
 **************************************************************************/
const Global_1 = require("../src/Global");
const sidenav_1 = __importDefault(require("../sidenav"));
describe('sidenav', () => {
    const evt = true;
    const handler = () => { };
    const sidenavMock = {
        collapsed: true,
        emit: (type, evt) => {
            return { evt, type };
        },
        get: (param) => {
            return Promise.resolve(param);
        },
        off: (type, handler) => {
            return { handler, type };
        },
        on: (type, handler) => {
            return { handler, type };
        },
        // @ts-expect-error - ignore for test
        onNavItemClick: (callback) => { }, // eslint-disable-line @typescript-eslint/no-unused-vars
        useLeftNavButton: (param) => {
            return param;
        },
        visible: true
    };
    beforeEach(() => {
        const emr = (0, Global_1.getEMR)();
        jest.spyOn(emr, 'sidenav').mockReturnValue(sidenavMock);
    });
    afterEach(() => jest.clearAllMocks());
    it('can emit()', () => {
        const spy = jest.spyOn(sidenavMock, 'emit');
        // action
        sidenav_1.default.emit('change:shellSideNavCollapsed', evt);
        // verify
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('change:shellSideNavCollapsed', evt);
    });
    it('can get()', () => __awaiter(void 0, void 0, void 0, function* () {
        const spy = jest.spyOn(sidenavMock, 'get');
        // action
        yield Promise.all([
            sidenav_1.default.get('shellSideNavCollapsed')
        ]);
        // verify
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('shellSideNavCollapsed');
        // verify getters
        expect(sidenav_1.default.collapsed).toEqual(true);
        expect(sidenav_1.default.visible).toEqual(true);
    }));
    it('can off()', () => {
        const spy = jest.spyOn(sidenavMock, 'off');
        // action
        sidenav_1.default.off('change:shellSideNavCollapsed', handler);
        // verify
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('change:shellSideNavCollapsed', handler);
    });
    it('can on()', () => {
        const spy = jest.spyOn(sidenavMock, 'on');
        // action
        sidenav_1.default.on('change:shellSideNavCollapsed', handler);
        // verify
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('change:shellSideNavCollapsed', handler);
    });
    it('can onNavItemClick()', () => {
        const spy = jest.spyOn(sidenavMock, 'onNavItemClick');
        // @ts-expect-error - ignore for test
        const callback = (item) => { }; // eslint-disable-line @typescript-eslint/no-unused-vars
        // action
        sidenav_1.default.onNavItemClick(callback);
        // verify
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith(callback);
    });
    it('can useLeftNavButton()', () => {
        const spy = jest.spyOn(sidenavMock, 'useLeftNavButton');
        const config = {
            callback: (isOpen) => console.log('side nav state is now ', isOpen), // eslint-disable-line no-console
            closeOnSelection: false
        };
        // action
        sidenav_1.default.useLeftNavButton(config);
        // verify
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith(config);
    });
    it('can useLeftNavButton({enabled: false}) to remove the button', () => {
        const spy = jest.spyOn(sidenavMock, 'useLeftNavButton');
        // action
        sidenav_1.default.useLeftNavButton({ enabled: false });
        // verify
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith({ enabled: false });
    });
    it('can re-enable button after useLeftNavButton({enabled: false})', () => {
        const spy = jest.spyOn(sidenavMock, 'useLeftNavButton');
        const config = {
            callback: (isOpen) => console.log('side nav state is now ', isOpen), // eslint-disable-line no-console
            closeOnSelection: false,
            enabled: true
        };
        // action
        sidenav_1.default.useLeftNavButton({ enabled: false });
        sidenav_1.default.useLeftNavButton(config);
        // verify
        expect(spy).toHaveBeenCalledTimes(2);
        expect(spy).toHaveBeenNthCalledWith(2, config);
    });
});
//# sourceMappingURL=sidenav.test.js.map