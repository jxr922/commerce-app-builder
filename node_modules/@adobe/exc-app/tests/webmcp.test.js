"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
/*************************************************************************
 * Copyright 2026 Adobe
 * All Rights Reserved.
 *
 * NOTICE: Adobe permits you to use, modify, and distribute this file in
 * accordance with the terms of the Adobe license agreement accompanying
 * it. If you have received this file from a source other than Adobe,
 * then your use, modification, or distribution of it requires the prior
 * written permission of Adobe.
 **************************************************************************/
const Global_1 = require("../src/Global");
const webmcp_1 = __importDefault(require("../webmcp"));
describe('webmcp', () => {
    const definition = {
        description: 'Returns the title of the current page.',
        execute: () => __awaiter(void 0, void 0, void 0, function* () { return ({ title: 'Test' }); }),
        inputSchema: {
            properties: {},
            type: 'object'
        },
        name: 'getPageTitle'
    };
    const serializableToolDefinition = {
        description: definition.description,
        inputSchema: definition.inputSchema,
        name: definition.name
    };
    const webmcpMock = {
        executeTool: (_name, _args) => Promise.resolve({}), // eslint-disable-line @typescript-eslint/no-unused-vars
        getTools: () => Promise.resolve([serializableToolDefinition]),
        off: (_event, _handler) => { }, // eslint-disable-line @typescript-eslint/no-unused-vars
        on: (_event, _handler) => { }, // eslint-disable-line @typescript-eslint/no-unused-vars
        registerTool: (_definition) => { }, // eslint-disable-line @typescript-eslint/no-unused-vars
        unregisterTool: (_name, _component) => { } // eslint-disable-line @typescript-eslint/no-unused-vars
    };
    beforeEach(() => {
        jest.clearAllMocks();
        const emr = (0, Global_1.getEMR)();
        emr.webmcp = () => webmcpMock;
        jest.spyOn(emr, 'webmcp');
    });
    it('registerTool() delegates to the implementation', () => {
        const spy = jest.spyOn(webmcpMock, 'registerTool');
        webmcp_1.default.registerTool(definition);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith(definition);
    });
    it('unregisterTool() delegates to the implementation', () => {
        const spy = jest.spyOn(webmcpMock, 'unregisterTool');
        webmcp_1.default.unregisterTool('getPageTitle');
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('getPageTitle', undefined);
    });
    it('unregisterTool() delegates with component', () => {
        const spy = jest.spyOn(webmcpMock, 'unregisterTool');
        webmcp_1.default.unregisterTool('getContext', 'damPanel');
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('getContext', 'damPanel');
    });
    it('executeTool() delegates with name only', () => {
        const spy = jest.spyOn(webmcpMock, 'executeTool');
        webmcp_1.default.executeTool('getPageTitle');
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('getPageTitle', undefined);
    });
    it('executeTool() delegates with args', () => {
        const spy = jest.spyOn(webmcpMock, 'executeTool');
        const args = { foo: 'bar' };
        webmcp_1.default.executeTool('getPageTitle', args);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('getPageTitle', args);
    });
    it('executeTool() returns the underlying promise', () => __awaiter(void 0, void 0, void 0, function* () {
        jest.spyOn(webmcpMock, 'executeTool').mockResolvedValue({ title: 'Page' });
        const result = yield webmcp_1.default.executeTool('getPageTitle');
        expect(result).toEqual({ title: 'Page' });
    }));
    it('getTools() delegates to the implementation', () => __awaiter(void 0, void 0, void 0, function* () {
        jest.spyOn(webmcpMock, 'getTools').mockResolvedValue([serializableToolDefinition]);
        const tools = yield webmcp_1.default.getTools();
        expect(webmcpMock.getTools).toHaveBeenCalledTimes(1);
        expect(tools).toEqual([serializableToolDefinition]);
    }));
    it('on() delegates to the implementation', () => {
        const spy = jest.spyOn(webmcpMock, 'on');
        const handler = jest.fn();
        webmcp_1.default.on('change:tools', handler);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('change:tools', handler);
    });
    it('off() delegates to the implementation', () => {
        const spy = jest.spyOn(webmcpMock, 'off');
        const handler = jest.fn();
        webmcp_1.default.off('change:tools', handler);
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('change:tools', handler);
    });
});
//# sourceMappingURL=webmcp.test.js.map