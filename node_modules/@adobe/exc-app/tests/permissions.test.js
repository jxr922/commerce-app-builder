"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
/*************************************************************************
 * Copyright 2021 Adobe
 * All Rights Reserved.
 *
 * NOTICE: Adobe permits you to use, modify, and distribute this file in
 * accordance with the terms of the Adobe license agreement accompanying
 * it. If you have received this file from a source other than Adobe,
 * then your use, modification, or distribution of it requires the prior
 * written permission of Adobe.
 **************************************************************************/
const Global_1 = require("../src/Global");
const permissions_1 = __importDefault(require("../permissions"));
describe('permissions', () => {
    let getSpy;
    beforeAll(() => {
        const emr = (0, Global_1.getEMR)();
        const permissionsMock = { get: jest.fn() };
        jest.spyOn(emr, 'permissions').mockReturnValue(permissionsMock);
        getSpy = jest.spyOn(permissionsMock, 'get');
    });
    afterEach(() => jest.clearAllMocks());
    it('calls runtime permissions service with permissions', () => __awaiter(void 0, void 0, void 0, function* () {
        getSpy.mockImplementationOnce(() => Promise.resolve({ permissions: {
                permission1: ['*'],
                permission2: ['read', 'write']
            } }));
        const { permissions } = yield permissions_1.default.get({ permissions: ['permission1', 'permission2'] });
        expect(getSpy).toHaveBeenCalledTimes(1);
        expect(getSpy).toHaveBeenCalledWith({ permissions: ['permission1', 'permission2'] });
        expect(permissions).toEqual({
            permission1: ['*'],
            permission2: ['read', 'write']
        });
    }));
    it('calls runtime permissions service with resourceTypes', () => __awaiter(void 0, void 0, void 0, function* () {
        getSpy.mockImplementationOnce(() => Promise.resolve({ resourceTypes: {
                permission1: ['*'],
                permission2: ['read', 'write']
            } }));
        const { resourceTypes } = yield permissions_1.default.get({ resourceTypes: ['permission1', 'permission2'] });
        expect(getSpy).toHaveBeenCalledTimes(1);
        expect(getSpy).toHaveBeenCalledWith({ resourceTypes: ['permission1', 'permission2'] });
        expect(resourceTypes).toEqual({
            permission1: ['*'],
            permission2: ['read', 'write']
        });
    }));
    it('calls runtime permissions service with resourceTypes', () => __awaiter(void 0, void 0, void 0, function* () {
        getSpy.mockImplementationOnce(() => Promise.resolve({
            permissions: {
                permission1: ['*'],
                permission2: ['read', 'write']
            },
            resourceTypes: {
                permission1: ['*'],
                permission2: ['read', 'write']
            }
        }));
        const res = yield permissions_1.default.get({
            permissions: ['permission1', 'permission2'],
            resourceTypes: ['permission1', 'permission2']
        });
        expect(getSpy).toHaveBeenCalledTimes(1);
        expect(getSpy).toHaveBeenCalledWith({
            permissions: ['permission1', 'permission2'],
            resourceTypes: ['permission1', 'permission2']
        });
        expect(res).toEqual({
            permissions: {
                permission1: ['*'],
                permission2: ['read', 'write']
            },
            resourceTypes: {
                permission1: ['*'],
                permission2: ['read', 'write']
            }
        });
    }));
});
//# sourceMappingURL=permissions.test.js.map