"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
/*************************************************************************
 * Copyright 2020 Adobe
 * All Rights Reserved.
 *
 * NOTICE: Adobe permits you to use, modify, and distribute this file in
 * accordance with the terms of the Adobe license agreement accompanying
 * it. If you have received this file from a source other than Adobe,
 * then your use, modification, or distribution of it requires the prior
 * written permission of Adobe.
 **************************************************************************/
const Global_1 = require("../src/Global");
const user_1 = __importDefault(require("../user"));
describe('user', () => {
    const evt = '';
    const handler = () => { };
    const userMock = {
        authExpired: () => { },
        emit: (type, evt) => {
            return { evt, type };
        },
        generateSubOrg: () => {
            return Promise.resolve('sub-org');
        },
        get: (param) => {
            return Promise.resolve(param);
        },
        getAEMInstances: () => {
            return Promise.resolve([{
                    domain: 'domain',
                    environment: 'qa',
                    path: 'path',
                    rootTemplate: 'template',
                    title: 'title',
                    type: 'storage'
                }]);
        },
        getFulfillableItems: (serviceCode) => {
            return Promise.resolve([`${serviceCode}_fi`]);
        },
        getLastLogin: () => {
            return Promise.resolve([]);
        },
        logoutUrl: 'https://localhost/logout',
        off: (type, handler) => {
            return { handler, type };
        },
        on: (type, handler) => {
            return { handler, type };
        },
        rolesPickerConfig: undefined,
        showLanguagePicker: undefined,
        showRolesPicker: undefined
    };
    const emr = (0, Global_1.getEMR)();
    beforeEach(() => {
        jest.spyOn(emr, 'user').mockReturnValue(userMock);
    });
    it('can emit()', () => {
        const spy = jest.spyOn(userMock, 'emit');
        // action
        user_1.default.emit('change:theme', evt);
        // verify
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('change:theme', evt);
    });
    it('can get()', () => __awaiter(void 0, void 0, void 0, function* () {
        const spy = jest.spyOn(userMock, 'get');
        // action
        yield Promise.all([
            user_1.default.get('collaborator'),
            user_1.default.get('instance'),
            user_1.default.get('imsOrg'),
            user_1.default.get('imsOrgName'),
            user_1.default.get('imsOrgs'),
            user_1.default.get('imsToken'),
            user_1.default.get('imsProfile'),
            user_1.default.get('locale'),
            user_1.default.get('preferredLanguages'),
            user_1.default.get('processedImsOrg'),
            user_1.default.get('s2Theme'),
            user_1.default.get('sandbox'),
            user_1.default.get('sandboxes'),
            user_1.default.get('subOrg'),
            user_1.default.get('tenant'),
            user_1.default.get('theme'),
            user_1.default.get('userCollaborators')
        ]);
        // verify
        expect(spy).toHaveBeenCalledTimes(17);
        expect(spy).toHaveBeenCalledWith('collaborator');
        expect(spy).toHaveBeenCalledWith('instance');
        expect(spy).toHaveBeenCalledWith('imsOrg');
        expect(spy).toHaveBeenCalledWith('imsOrgName');
        expect(spy).toHaveBeenCalledWith('imsOrgs');
        expect(spy).toHaveBeenCalledWith('imsToken');
        expect(spy).toHaveBeenCalledWith('imsProfile');
        expect(spy).toHaveBeenCalledWith('locale');
        expect(spy).toHaveBeenCalledWith('preferredLanguages');
        expect(spy).toHaveBeenCalledWith('processedImsOrg');
        expect(spy).toHaveBeenCalledWith('s2Theme');
        expect(spy).toHaveBeenCalledWith('sandbox');
        expect(spy).toHaveBeenCalledWith('sandboxes');
        expect(spy).toHaveBeenCalledWith('subOrg');
        expect(spy).toHaveBeenCalledWith('tenant');
        expect(spy).toHaveBeenCalledWith('theme');
        expect(spy).toHaveBeenCalledWith('userCollaborators');
        // Verify logoutUrl getter
        expect(user_1.default.logoutUrl).toEqual('https://localhost/logout');
    }));
    it('can generateSubOrg()', () => {
        const spy = jest.spyOn(userMock, 'generateSubOrg');
        user_1.default.generateSubOrg({
            global_company_id: 'suborg1',
            ident: 'BBWWP9TCRVXT18TDX7ZG6TCPH4',
            owningEntity: '6A29340459CA8D350A49413A@AdobeOrg',
            serviceCode: 'dma_test'
        });
        expect(spy).toHaveBeenCalledTimes(1);
    });
    it('can getFulfillableItems()', () => {
        const spy = jest.spyOn(userMock, 'getFulfillableItems');
        user_1.default.getFulfillableItems('acp');
        expect(spy).toHaveBeenCalledTimes(1);
    });
    it('can getLastLogin()', () => {
        const spy = jest.spyOn(userMock, 'getLastLogin');
        user_1.default.getLastLogin();
        expect(spy).toHaveBeenCalledTimes(1);
        user_1.default.getLastLogin({ appIds: ['app1', 'app2'] });
        expect(spy).toHaveBeenCalledTimes(2);
        expect(spy).toHaveBeenLastCalledWith({ appIds: ['app1', 'app2'] });
    });
    it('can getAEMInstances()', () => {
        const spy = jest.spyOn(userMock, 'getAEMInstances');
        user_1.default.getAEMInstances();
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenLastCalledWith(undefined);
        user_1.default.getAEMInstances({ filterByRepoParam: true });
        expect(spy).toHaveBeenCalledTimes(2);
        expect(spy).toHaveBeenLastCalledWith({ filterByRepoParam: true });
    });
    it('can off()', () => {
        const spy = jest.spyOn(userMock, 'off');
        // action
        user_1.default.off('change:imsOrg', handler);
        // verify
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('change:imsOrg', handler);
    });
    it('can on()', () => {
        const spy = jest.spyOn(userMock, 'on');
        // action
        user_1.default.on('change:tenant', handler);
        // verify
        expect(spy).toHaveBeenCalledTimes(1);
        expect(spy).toHaveBeenCalledWith('change:tenant', handler);
    });
    it('can call authExpired', () => {
        const user = emr.user();
        const authExpiredSpy = jest.spyOn(user, 'authExpired');
        user.authExpired();
        expect(authExpiredSpy).toHaveBeenCalledTimes(1);
    });
});
//# sourceMappingURL=user.test.js.map